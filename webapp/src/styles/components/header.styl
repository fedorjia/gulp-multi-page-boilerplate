.app-header {
	position: relative;
	text-align: right;
	margin-left: auto;
	margin-right: auto;
	width: 100%;
	border-bottom: 1px solid #f1f1f1;
	clearfix();

	.logo {
		float: left;
		margin-left: 18px;
		.icon-logobeta {
			font-size: 4rem;
			color: #585858;
		}
	}
}